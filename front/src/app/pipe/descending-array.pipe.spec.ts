import { DescendingArrayPipe } from './descending-array.pipe';

describe('DescendingArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new DescendingArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
